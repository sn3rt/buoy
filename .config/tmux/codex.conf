# Minimal config for the Codex popup tmux server.

set -g status off
set -g mouse on
set -sg escape-time 10

# Hide the popup by detaching the client (outer popup auto-closes via -E).
bind-key -n M-c detach-client
