# Minimal config for the Yazi popup tmux server.

set -g status off
set -g mouse on
set -sg escape-time 10
set -g destroy-unattached on

# Yazi is a short-lived picker. Close its popup session instead of detaching,
# otherwise tmux can leave a stuck popup showing "[detached from yazi]".
bind-key -n M-c kill-session
